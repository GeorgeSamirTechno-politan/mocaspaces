<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/amenity_image_view"
        android:layout_width="@dimen/_25sdp"
        android:layout_height="@dimen/_25sdp"
        android:layout_marginHorizontal="@dimen/_20sdp"
        android:src="@drawable/ic_moca_m"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:visibility="gone"
        android:layout_marginTop="@dimen/_3sdp"
        android:id="@+id/amenity_text_view"
        style="@style/medium"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="@dimen/_7sdp"
        app:layout_constraintEnd_toEndOf="@id/amenity_image_view"
        app:layout_constraintStart_toStartOf="@id/amenity_image_view"
        app:layout_constraintTop_toBottomOf="@id/amenity_image_view"
        tools:text="Wifi" />

</androidx.constraintlayout.widget.ConstraintLayout>